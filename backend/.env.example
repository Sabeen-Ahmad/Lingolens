PORT=3000

# Option A (production) – paste the entire JSON as a single line
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# Option B (local dev) – leave this empty and place serviceAccountKey.json in backend/
